from distutils.core import setup, Extension
from distutils.command import build_ext
import os
"""
def read(*filenames, **kwargs):
    encoding = kwargs.get('encoding', 'utf-8')
    sep = kwargs.get('sep', '\n')
    buf = []
    for filename in filenames:
        with io.open(filename, encoding=encoding) as f:
            buf.append(f.read())
    return sep.join(buf)

long_description = read('README.md')
"""

class my_build_ext(build_ext.build_ext):
	def build_extension(self, ext):
		import shutil
		import os.path
		import pyQCD.core.lattice

		module_file = pyQCD.core.lattice.__file__

		shutil.copyfile(module_file,
						self.get_ext_fullpath(ext.name))

setup(
    name='pyQCD',
    version='${PACKAGE_VERSION}',
    packages=['pyQCD',
			  'pyQCD.core',
			  'pyQCD.bin',
			  'pyQCD.interfaces',
			  'pyQCD.postprocessing'],
	cmdclass = {'build_ext': my_build_ext},
	ext_modules = [Extension('pyQCD/core/lattice',
							 sources = ['pyQCD/core/src/wrapper.cpp'])],
	package_dir={'': '${CMAKE_CURRENT_SOURCE_DIR}'},
)
"""
setup(
    name='pyQCD',
    version='${PACKAGE_VERSION}',
    url='http://github.com/mspraggs/pyqcd/',
    license='Apache Software License',
    author='Matt Spraggs',
    install_requires=['numpy', 'scipy',
                    ],
    author_email='matthew.spraggs@gmail.com',
    description='pyQCD provides a Python library for running coarse lattice '
	'QCD simulations on desktop and workstation computers.',
    long_description=long_description,
    packages=['pyQCD'],
	package_dir={'': '.'},
    include_package_data=True,
    platforms='any',
    classifiers = [
        'Programming Language :: Python',
		'Programming Language :: C++',
        'Development Status :: 3 - Alpha',
        'Natural Language :: English',
		'Intended Audience :: Science/Research',
        'License :: OSI Approved :: GNU General Public License v3 (GPLv3)',
        'Operating System :: OS Independent',
		'Topic :: Scientific/Engineering :: Physics',
        ],
)"""
